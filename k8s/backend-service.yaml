apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: fingrid-dashboard
  labels:
    app: backend
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: backend
  type: ClusterIP
